# 本次项目调整

> 日期：2026-08-12  
> 基线：`main` / `f7c736d82c31d098d1573e7892ffa27b39bcf3a1`  
> 调整性质：完整审计后的文档—代码语义对齐  
> 状态：文档调整完成，运行时改进进入后续实施清单

## 1. 本次目标

检查项目设计是否偏离，并让公开文档对 0.1 的描述与实际 Schema、TypeScript 实现、测试和发布门禁一致。交付一份可执行的差异设计和一份本轮变更记录。

验收条件：

- 根目录存在 `设计方案及差异.md` 与 `本次项目调整.md`。
- 两份文档明确区分当前保证、人工/Agent 审查和未来目标。
- README、知识模型、治理流程及 Agent 手册不再把未实现能力描述成 Core 0.1 的自动保证。
- 新文档进入公开发布白名单，并通过格式、链接、隐私、边界和公开项目检查。

非目标：本轮不修改公共 Schema、CLI、库 API、私有 Application 运行时，不发布 npm/GitHub Release，不部署或连接真实环境。

## 2. 审计结论摘要

核心架构未发生方向性偏离，当前代码已经形成可运行的 0.1 结构层。偏差主要是文档把下列目标提前写成了现状：

- 契约文件的存在、版本和格式校验；
- 治理 Policy、审批、Scenario 与证据的执行；
- Package 依赖和 Artifact relation 的传递影响分析；
- 完整公开传递闭包和任意文件类型的隐私防护；
- 本地 Application 对权益、遥测、显式状态及所有声明场景的运行时证明。

安全与质量方面还发现：公开目录前缀与文本扩展名扫描之间存在空档；循环 YAML 对象可使秘密扫描栈溢出；嵌套敏感字段可绕过启发式扫描；本地小程序的第二条研究内容、每日投票语义、构建完整性、ARIA 属性和颜色对比度需要修复。当前工作树和历史未发现实际私有内容泄漏，依赖审计也未发现已知漏洞。

完整证据、优先级和目标方案见 [`设计方案及差异.md`](设计方案及差异.md)。

## 3. 本次实际调整

### 3.1 新增交付文档

- `设计方案及差异.md`：记录事实基线、审计证据、13 项差异、目标架构、兼容策略、实施顺序和完成定义。
- `本次项目调整.md`：记录本轮范围、已修改内容、验证结果、剩余风险和下一步入口。

### 3.2 对齐现有公开文档

- 根 README：把“确定性反向影响”收敛为当前实际支持的直接 Package 消费者查询；把校验范围改成准确列举；修复规范 `DomainArtifact` 示例缺少必填 `contractRefs`；增加本次两份文档入口和 0.1 限制说明。
- `packages/core/README.md`：明确当前契约、Scenario、治理和影响分析边界，避免把引用声明误写成契约内容已验证。
- `docs/knowledge-model.md`：把 provenance 与 relation 字段说明对齐 Schema；明确场景、测试和审批在 0.1 中是声明或外部流程，而非 Core 自动执行。
- 示例治理文档：把完整发布流程标记为目标治理流程，并列出 Core 0.1 自动执行的子集。
- Validator/Impact Analyst 手册：分别标记 Core 自动证据与人工/Agent 分析，禁止把传递或语义判断冒充 CLI 输出。

### 3.3 对齐发布边界

- 在 `public.release.json` 中精确加入两份新文档。
- 修复 `scripts/public-boundary-check.mjs` 对 Git 引号/八进制转义路径的误解析：改用 NUL 分隔的原始路径输出，使中文文件名能按真实路径接受白名单校验，也兼容文件名中的换行等逐行解析边界。
- 没有扩大 `private/` 或 `.local/` 的公开范围。

### 3.4 有意未改动

- 未修改 `packages/core/src/`、`packages/core/schemas/` 和测试语义。
- 未修改被忽略的本地 Application。
- 未调整版本号、tag、依赖锁、远端设置或发布物。

这样可以先建立准确、可评审的设计基线，再按优先级实施会影响公共契约的改动，避免一次文档任务静默改变使用者行为。

## 4. 后续实施清单

| 批次 | 优先级 | 内容                                                               | 进入条件                                   |
| ---- | ------ | ------------------------------------------------------------------ | ------------------------------------------ |
| A    | P0     | 公开文件分类、循环对象防护、深度/大小预算、嵌套秘密扫描            | 下一次公开候选或接收不可信输入前           |
| B    | P1     | 契约注册、Scope/生命周期闭包、Policy/Scenario 证据、版本化传递影响 | 发布 0.2 Schema/API 前                     |
| C    | P1     | 本地 Application 权益、遥测、状态机、fixture、每日语义、场景证据   | 把本地 Application 称为 Harness 合规实现前 |
| D    | P1/P2  | 小程序完整构建与无障碍；许可证、SBOM、provenance 和签名            | 对外分发可安装包前                         |

建议下一次代码调整只选择一个批次，先为构造失败样例建立测试，再改实现。批次 A 不依赖业务决策，适合作为首个实施项。

## 5. 验证记录

本次交付完成后运行以下本地检查；最终结果以同一工作树的命令输出为准：

| 命令                       | 目的                                        | 结果                                                   |
| -------------------------- | ------------------------------------------- | ------------------------------------------------------ |
| `npx prettier --check ...` | 检查所有本次修改文件格式                    | 通过                                                   |
| `npm run docs:check`       | 检查公开 Markdown 链接                      | 通过；21 份 Markdown                                   |
| `npm run privacy:check`    | 检查公开文本的已知私有标识和秘密模式        | 通过；110 个公开文本文件                               |
| `npm run boundary:check`   | 检查 Git/public release 边界                | 通过；113 个候选文件；同时作为中文路径回归检查         |
| `npm run check:public`     | 重跑 Core、公开示例、文档、隐私和边界总门禁 | 通过；TypeScript、构建、Core 11 项测试和示例校验均通过 |

审计阶段已经通过 `npm run check:local`：Core 11 项测试、本地 Application 25 项测试，以及公开示例、文档、隐私和边界检查全部通过。根项目与本地 Application 的生产/完整依赖审计均为 0 个已知漏洞。

## 6. 剩余风险

本轮让描述与事实一致，但没有消除 `设计方案及差异.md` 中 D-01 至 D-12 的实现差异。尤其在完成批次 A 前：

- 不应把现有公开门禁理解为能检查任意二进制内容；
- 不应直接校验来源不可信、规模不受控的 YAML/JSON；
- 不应把秘密扫描作为唯一的密钥防护；
- 不应把本地 Application 的 fixture 测试当作真实平台或生产合规证据。

## 7. 变更文件

最终纳入本次交付的文件：

- `设计方案及差异.md`
- `本次项目调整.md`
- `README.md`
- `README.zh-CN.md`
- `packages/core/README.md`
- `docs/knowledge-model.md`
- `examples/broker-digital-channel/README.zh-CN.md`
- `examples/broker-digital-channel/docs/knowledge-governance.md`
- `examples/broker-digital-channel/agents/validator.md`
- `examples/broker-digital-channel/agents/impact-analyst.md`
- `public.release.json`
- `scripts/public-boundary-check.mjs`

运行时、Schema 或私有 Application 变更必须拆分到后续实施。
