# 本次项目调整

> 日期：2026-08-12
>
> 分支：`codex/factory-0.4-governance-identity`
>
> 实施基线：`main@3a41e1191883278c529409426142b3ab73982632`
>
> 目标版本：Core `0.2.0` / Factory `0.4.0` / Factory protocol `coga.dev/factory/v0.3`
>
> 状态：0.2 与 Factory 0.3 已合并；Factory 0.4 正在关闭机器交付身份与人类审批身份混同。允许开发分支、PR、公开 Linux CI、GitHub App 安装和 Factory Draft PR 治理验证；仍不发布 npm、不打 tag、不创建 Release、不部署、不自动合并生成候选

## 1. 目标与边界

前序 0.2 已关闭 D-01 至 D-10、D-11 静态部分并建立 D-12 发布候选门禁；Factory 0.3 已关闭 D-15 的版本化 Agent Proposal Receipt、双 Application fan-out 与远端证据闭环。本轮交付 Factory 0.4：把交付主体固定为仓库安装的 GitHub App，把 Policy 审批固定为与机器身份分离的人类账号，并保持最强自动动作仅为 Draft-to-ready-for-review。

验收要求：除既有本地执行和证据闭包外，真实候选 PR 必须由 Work Order 声明的 `<app-slug>[bot]` 创建；授权人类在精确 head 上提交带 Policy marker 的 APPROVED review；五项公开检查与 artifact attestation 全部成功；collector 复核后只能把 Draft 提升为 ready，不能自动合并。

非目标：不接真实后端/数据/业务密钥，不伪造微信 DevTools、真机、模型提供者或 Human Policy 证据，不执行 npm 首发、版本 tag、GitHub Release 或部署，不改变 Core 的“只验证记录、不执行任意证据命令”边界。GitHub App 私钥和短期 installation token 不进入仓库、Work Order、证据或模型上下文。Factory 生成的 Application PR 只能保持 Draft 或在真实证据齐全后变为 ready-for-review，绝不自动合并。

## 2. 公共契约调整

- v0.2 是破坏性 envelope，只接受 `schemaVersion: coga.dev/v0.2`，不双读 v0.1。
- 所有资源显式声明 scope、visibility 和 attestations；审批记录绑定精确 Policy、approver、approvedAt 与非空 evidence。
- Artifact relation 与 Validation target 使用 `{id, version}`；`passed/failed` 必须有 checkedAt、validator 和 evidence，`pending` 不伪装完成证据。
- Contract reference 固定包含 id、version、path、format，可携带 `sha256:<hex>`；release Profile 要求摘要。契约根声明 `x-coga-contract: {id, version}`。
- Instance 使用 `governance.approvalRules`，把 lifecycle 绑定到一组精确 Policy；`governance.releaseEvidence` 保存受控 lifecycle 的发布证据定位符。
- `local`、`public`、`release` 三档 Profile 共享显式 options；已装载对象不能用不同 options 复用。
- CLI 的 Profile 参数顺序无关；impact 查询改为精确 `artifact.id@version`，返回 direct/transitive/older-pin 路径及 Scenario/Runbook 重跑集合。

## 3. 本轮实际调整

### 3.1 Core 与公开门禁

- Canonical 文件、Contract 文件、深度、逻辑节点和 YAML alias 使用固定预算；循环、特殊对象和越界路径转换为稳定 `load.*` / `contract.*` issue。
- 秘密扫描继承祖先敏感上下文并规范化键名，覆盖 password、secret、token、apiKey、private/access key、clientSecret、authorization 与 credential(s)。
- 公开 boundary/privacy 共用 Git NUL 候选枚举和内容分类：受控 UTF-8 文本、图片/PDF 文件头与大小、总候选体积均受限；ZIP 和未知类型失败。
- 二进制业务内容不做自动隐私理解，这是明确接受的残余风险，仍需要人工检查。

### 3.2 契约、治理与资源图

- 校验 JSON Schema Draft 2020-12、OpenAPI 3.1.x、契约身份/版本、只读本地 `$ref` 闭包和 release SHA-256；`$ref` 使用循环安全的迭代遍历并共享大小、节点与深度预算。
- 建立 Instance、Package、Application、Artifact、Contract、Policy 的精确图，检查 Package DAG、Scope/生命周期、跨 Package relation 的依赖路径、Application Scenario/Runbook 可达性和 Policy 类型。
- release Profile 校验 completed validation evidence、approval attestation 和 lifecycle 发布证据的完整性；Core 不执行 Scenario、测试、评审或审批命令。
- impact 使用版本化路径，按 Application 去重并保留多个原因；older-pin 只基于当前 Instance 已注册的其他 Package 版本。

### 3.3 数据、fixture 与文档迁移

- 公开 Instance 的原有 5 个 Package、30 个 Artifact、2 个 Application、6 个 JSON Schema Contract 与表单契约统一迁移到 0.2.0；Platform Package 补充 Domain Package 依赖。Factory 增量再加入 1 个 H5 Platform Package 与 2 个 Artifact，当前共 6 个 Package、32 个 Artifact。
- 公开示例实盘 60 条无可追溯证据的 `passed` 全部降为 `pending`；19 个相关 Artifact 从 `published` 降为 `approved`。
- Core valid fixture 的 3 条 `passed` 保留并补充真实本地 Markdown evidence；新增真实 OpenAPI 3.1 Contract 和 approved Policy，published fixture 资源携带精确审批 attestation。
- 早期审计中的“62 条”是混合盘点估计；最终 tracked 盘点为“公开无证据 60 条 + Core fixture 可补证据 3 条”，本轮没有为凑数制造记录。
- 7 个最终 Contract 文件按原始字节计算 SHA-256，共写入公开示例、有效 fixture 和本地 Application 的 15 个引用。若后续重新格式化 Contract 文件，必须重新计算摘要。
- 前序候选先迁移本地私有 manifest；本次在被 Git 忽略的 `private/application/` 中补齐权益、遥测、七态、第二研究详情、每日投票持久化、完整静态构建和场景证据映射。
- README、知识模型、Core/示例说明、治理与 Agent 手册、CHANGELOG 和设计差异状态统一更新到 0.2；没有新增第三份迭代文档。

### 3.4 Application conformance

- 权益快照在进入业务判断前校验 issuer、audience、结构、有效期和 fixture 完整性标记；过期、伪造、畸形、矛盾和敌意输入全部默认拒绝且不抛异常。Fixture 由注入时钟动态签发 24 小时，避免固定日期失效。
- 遥测只允许注册事件和注册属性，强制 owner、purpose、schemaVersion、retention、classification 信封；旧污染字段不能在后续写入中存活。
- 页面采用 `loading / empty / ready / stale / denied / recoverable-error / terminal-error` 七态，研究与策略权限门显式进入 `denied`，只有可恢复错误可重试。
- 两条研究目录记录都有详情；每日投票按主体、洞察、自然日写入可注入存储，覆盖同日重启、跨日和损坏存储。
- 6 个 manifest 场景全部映射：5 个绑定稳定测试 ID，生产事故协同保留 manual 和非空原因。
- 小程序基础库固定 `3.7.12`；构建验证 6 个页面、2 个组件、37 个产物；静态 ARIA 和普通文本色板 4.5:1 检查通过。

### 3.5 发布成熟度

- Core 包新增与根许可证字节一致的 `LICENSE`，公开内容策略显式分类该无扩展名文件。
- 发布工具用 npm 内建能力生成 CycloneDX 1.5，并以 `package-lock.json` 独立重建生产依赖图，强制覆盖 5 个直接运行时依赖及传递闭包、排除 dev 依赖。
- 规范化 SBOM 移除随机 serial/timestamp，发布清单记录 tarball/SBOM 的大小、SHA-256、仓库、提交和可选 tag；验证器检查包身份、许可证、清单、SBOM 节点/边和目录精确闭包。
- 独立 `release.yml` 只响应版本 tag；要求 GitHub 验证的 annotated 签名 tag、tag commit、`GITHUB_SHA` 和显式刷新后的 `origin/main` tip 一致，并先生成/验证两次相同载荷。只读 job 不保留 checkout 凭据；高权限 job 不 checkout 或执行仓库代码，只验证跨 job SHA-256 handoff，再创建 GitHub artifact attestations 与待管理员复核的 draft 候选。普通 CI 不再响应 tag。
- 工作流不会把 draft 自动公开为可变 Release。tag signer 的授权规则、immutable release 仓库设置和最终发布由管理员在远端单独确认。
- 工作流没有 npm token 或 `npm publish`。`@coga/core` 当前 registry 查询为 404，而 npm 要求包已存在后才能配置 trusted publisher；首次包名占用/发布留给人类另行授权和 2FA bootstrap。
- 普通 PR CI 新增独立真实消费矩阵：从干净 `dist` 打包 Core 与 Factory `.tgz`、同时安装到空目录，通过已安装包验证 ESM API、`coga` / `coga-factory` bin 和 Schema exports；Linux 精确覆盖 Node.js 20.20.2、22.22.1 与 24.18.0。该 CI 不响应版本 tag。

### 3.6 Factory 0.3 提案、fan-out 与治理闭环

- `@coga/factory` 升至 `0.3.0`，只接受 Factory protocol `coga.dev/factory/v0.2`；不双读 v0.1。Core 与 Core resource envelope 保持 `0.2.0` / `coga.dev/v0.2`。
- 新增 `ProposalCompilation`、`AgentProposalReceipt`、`RemoteEvidence` 和派生 `GovernanceView`。Proposal Receipt 绑定模型/提供者/版本、提示模板和摘要、工具/网络/只读文件系统策略、预算、精确 Harness 输入文件摘要、规范化 Patch 与授权输出路径。
- Work Order 来自精确 Git 提交，绑定 Artifact 版本、domain patch、每个目标的 Proposal Receipt、完整 Application 目标、Policy、必需 checks、授权审阅者与独立 Draft PR 目标；不能携带 shell 命令。
- Planner 重新执行 Core public validation 与精确 impact，拒绝少报、多报或重复目标；参考变化 `web.h5.responsive.shell@0.2.0` 精确影响 Cedar 与 Birch 两个独立 H5 Application。
- Git 执行使用独立 worktree；补丁禁止越界、删除、rename/copy、symlink 和 submodule。恢复状态必须与每次从 Work Order 重新派生的完整目标 Plan 精确一致，并校验内容寻址工作区、分支、连续步骤前缀、回执和 worktree 身份。
- Node 测试/构建只用登记适配器，在固定镜像摘要、断网、只读根/仓库、非 root、无 capability、no-new-privileges、限 CPU/内存/进程/输出/时间的 Docker 中执行；构建写入独立临时挂载。
- 每个 Application 使用独立 worktree、分支、恢复状态、Evidence Bundle、提交和 Draft PR。一个目标失败返回 `partial` 并继续其他目标；相同 Work Order 重跑只恢复失败目标。
- Evidence Bundle 增加 Application 与 Proposal Receipt 语义摘要绑定；结构、语义、输入、Patch、状态或载荷篡改均失败。
- 新增真实、零依赖 Birch H5 参考 Application，与 Cedar 一起证明共享 Harness change 可 fan-out 到两个不同源码/测试候选。
- GitHub 适配器只创建/复用 open Draft PR。新增最小权限 `workflow_run` attestation job：不 checkout、不执行仓库代码，只接受同仓库成功 PR run、精确 open Draft head 和内容寻址 Evidence Bundle。
- Remote collector 先从本地精确提交复核 Work Order，再校验远端 Proposal Receipt、domain/proposal Patch 原始字节和完整路径闭包、Evidence Bundle、PR base/head、全部必需 checks、GitHub artifact attestation，以及授权审阅者在精确 head 上提交的 `[coga-policy:id@version]` APPROVED review。`--promote` 在最终 TOCTOU 复核后最多执行 Draft-to-ready-for-review；不 merge、publish、tag、release 或 deploy。
- Core 与 Factory tarball 现在同时从清空的 `dist` 安装到空目录，验证安装后的 ESM API、`coga` / `coga-factory` CLI 和全部 Schema exports；PR 的 Node 20/22/24 矩阵复用同一测试。

### 3.7 Factory 0.4 身份分离与真实治理门禁

- `@coga/factory` 升至 `0.4.0`，只接受 Factory protocol `coga.dev/factory/v0.3`；v0.1/v0.2 文档明确拒绝。交付适配器升级为 `github.app-draft-pr/v3`。
- Work Order 的 `delivery.identity` 必须声明 `kind: github-app`、精确 App slug 与固定 `COGA_FACTORY_GITHUB_TOKEN` 边界；不能把任意环境变量名变成取密钥接口。
- 交付前通过 GitHub installation 专用仓库枚举验证 token 类型和精确仓库访问；有界格式同时接受旧式和 `ghs_APPID_JWT` 无状态 installation token。安装 token 只进入清洗后的 Git/`gh` 子进程环境，不进入参数、remote URL、日志、文档或 Evidence；原始及编码凭据在失败信息中均脱敏。
- 凭据化 Git 禁用继承的 system/global config、credential helper、askpass、交互提示、hook 与 trace，拒绝命中目标端点的本地 URL rewrite，并在 push 前绑定本地 branch/head；PR 返回还必须绑定同仓库 base/head ref。Collector 固定 `github.com`，不继承可改变 `gh` host、API 路由、配置目录或调试输出的环境。
- 交付后必须证明 open Draft 的作者为 `<app-slug>[bot]`。PR author 写入 Remote Evidence，并在收集、最终 TOCTOU 复核和 ready 后确认三个位置持续一致。
- App slug 与 bot login 均不得出现在 `authorizedApprovers`；bot review 不可满足 Policy。人类 review 仍须来自允许名单、状态为 APPROVED、包含精确 `[coga-policy:id@version]`，并绑定当前 head。
- 仓库新增 `CODEOWNERS`，Work Order 的五项 required checks 与 `public-checks` job/matrix 由测试同步约束。远端 `main` 还须配置 1 个 CODEOWNER review、dismiss stale review、last-push approval 和相同五项必需检查。
- 负例覆盖缺 token、用户 token、无仓库权限安装、移动的本地 head/branch、Git URL rewrite、跨仓库 PR、既有/新建人类作者 PR、自审批、head/author TOCTOU 和凭据泄漏。自动化仍没有 merge、tag、Release、npm publish 或 deploy 能力。

## 4. 验证记录

工作轨迹已经取得的证据：

| 命令或检查                                    | 结果                                                                                    |
| --------------------------------------------- | --------------------------------------------------------------------------------------- |
| YAML/JSON 资源与引用盘点                      | 退出 0；确认公开 60 条 passed、19 个 published Artifact，Core valid fixture 3 条 passed |
| 机械 YAML v0.2 迁移                           | 退出 0；处理 56 个 canonical YAML 文件                                                  |
| Contract 摘要生成                             | 退出 0；7 个 Contract、15 个引用                                                        |
| `npx prettier --write`（迁移数据/fixture）    | 退出 0；OpenAPI 收敛后重新格式化并重算引用摘要                                          |
| `npm run build`                               | 退出 0                                                                                  |
| 公开示例和 Core valid fixture 的 release 校验 | 均退出 0                                                                                |
| `npm test`（Worker 中间候选）                 | 曾退出 1；旧 0.1 数量/API/CLI 断言随后已迁移，最终结果见下表                            |
| owned files `npx prettier --check`            | 退出 0                                                                                  |
| `npm run docs:check`                          | 退出 0；24 个 Markdown 文件                                                             |
| 公开示例、Core fixture、本地 Instance 校验    | release/release/local 三项均退出 0                                                      |
| 15 个 Contract 引用摘要复核                   | 退出 0；无不匹配                                                                        |
| `git diff --check`                            | 退出 0                                                                                  |

0.2 语义闭包提交 `ea9d7b7` 的整合结果：

| 最终命令                                     | 结果                                                                                               |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `npm run check:local`                        | 退出 0；Core 62/62、公开门禁 10/10、本地 Application 25/25，构建、文档、release/local 校验全部通过 |
| Node 20.20.2 下 Core 测试与 release CLI 校验 | 退出 0；Core 62/62，公开示例 release CLI 校验退出 0                                                |
| 根项目与本地 Application `npm audit`         | 两处均退出 0；各为 0 个已知漏洞                                                                    |
| `npm pack --dry-run --workspace @coga/core`  | 退出 0；`@coga/core@0.2.0`，59 个文件，51.3 kB 压缩/265.0 kB 解包；未生成发布物、未发布            |
| `git diff --check`                           | 退出 0                                                                                             |

Node 20.20.2 通过 `node@20.20.2` 的精确本地二进制执行；正式发布仍应在受控 CI 中复跑相同矩阵，但本轮没有操作远端 CI。

0.2 历史候选 `9b5eb43` 的本地证据保留如下，作为已经合并的 0.2 基线：

| 增量命令或检查                              | 结果                                                                                     |
| ------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 私有 Application `npm run typecheck`        | 退出 0                                                                                   |
| 私有 Application `npm test`                 | 退出 0；40/40                                                                            |
| 私有 Application `npm run build`            | 退出 0；37 个产物、6 个页面、2 个组件                                                    |
| 私有 Application `npm audit`                | 退出 0；0 个已知漏洞                                                                     |
| 私有 Instance `validate --profile local`    | 退出 0                                                                                   |
| `npm run release:test`                      | 退出 0；9/9，覆盖许可证、篡改/污染/预算拒绝、SBOM 闭包、tag CLI 与最小权限 workflow 门禁 |
| 三档 `npm run package:consumer-test`        | 退出 0；Node 20.20.2、22.22.1、24.18.0 均从空目录验证 ESM API、CLI 与 Schema exports     |
| `npm run check:local`                       | 退出 0；Core 62/62、公开门禁 11/11、私有 Application 40/40，文档/隐私/边界均通过         |
| Node 22.22.1 / 20.20.2 Core 测试            | 两档均退出 0；各 62/62；Node 20 release CLI 校验退出 0                                   |
| 根项目与私有 Application `npm audit`        | 两处均退出 0；0 个已知漏洞                                                               |
| `npm pack --dry-run --workspace @coga/core` | 退出 0；60 个文件、55,568 B 压缩/276,996 B 解包，含 10,845 B `LICENSE`                   |
| 根/包内 `LICENSE` SHA-256                   | 一致：`de860f4fb102c81d13287023085672f8d1d88495c38f7cbcc96184dfbef614da`                 |
| 微信开发者工具 CLI 非发布探测               | `open/auto/close/quit` 无可信输出并有界超时；未调用 preview/upload，未记为通过证据       |
| npm registry `@coga/core` 查询              | 404；证明 trusted publisher 不能在首发前配置，自动 npm 发布不属于本候选                  |

Factory 0.2 已合并能力与历史真实远端候选证据：

| 命令或检查                                                           | 结果                                                                                                                                                                    |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                                                  | 退出 0；Core 与 Factory 均通过                                                                                                                                          |
| `npm test`                                                           | 退出 0；当时 Core 62/62，Factory 8/8；Docker 专项在普通单测中显式跳过                                                                                                   |
| `npm run factory:e2e`                                                | 退出 0；真实 Docker 从精确提交完成 impact、两段补丁、Core 校验、H5 测试/构建、证据和候选提交                                                                            |
| Factory 恢复/安全/证据负向测试                                       | 退出 0；覆盖命令注入、越界、删除、symlink、零上下文补丁幂等、状态/计划/工作区身份和 Evidence Bundle 篡改                                                                |
| `npm run package:consumer-test`                                      | 退出 0；Core 与 Factory tarball 同时安装到空目录，ESM API、CLI 与 Schema exports 可用                                                                                   |
| Cedar H5 基线/Factory 提案测试与静态构建                             | 退出 0；基线不预置提案，Factory 应用补丁后新增 live status 断言并生成隔离构建输出                                                                                       |
| depth=2 合成 PR 浅克隆仿真                                           | 退出 0；两个直接父提交均处于 shallow graft 边界时，`check:public` 与真实 Docker `factory:e2e` 仍通过                                                                    |
| [实现 Draft PR #3](https://github.com/kayz/coga-core/pull/3)         | head `7b9a04b`；Linux [run 31591278283](https://github.com/kayz/coga-core/actions/runs/31591278283) 的 verify、Factory Docker 与 Node 20/22/24 消费共 5/5 通过          |
| [Factory 生成 Draft PR #6](https://github.com/kayz/coga-core/pull/6) | 0.2 最终 `main` 基线 `79bbb605` 生成；head `69efa046`，Linux [run 31593170046](https://github.com/kayz/coga-core/actions/runs/31593170046) 5/5 通过；仍为 Draft，未合并 |

Factory 0.3 合并候选及真实远端验证：

| 命令或检查                                                         | 结果                                                                                                                                                                                 |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npm run check:public`                                             | 退出 0；Core 62/62、Factory 21/21（Docker 专项显式分离）、公开门禁 12/12、release Profile、27 份文档链接及 207 个公开候选/文本隐私扫描全部通过                                       |
| `npm run factory:e2e`                                              | 退出 0；真实断网 Docker 对 Cedar/Birch 两个目标分别完成 domain/proposal Patch、Core 校验、Node 测试/构建、Evidence Bundle 与本地候选提交                                             |
| Factory 提案/fan-out/远端负向测试                                  | 退出 0；覆盖完整输入闭包、忽略产物、未绑定源码、Receipt/状态/计划/worktree 篡改、规划与执行失败隔离、只重试失败目标、缺 check/审批、原始二进制、PR 文件夹带/Patch 闭包和 head TOCTOU |
| `npm run package:consumer-test`                                    | 退出 0；空目录安装 Core/Factory tarball 后验证 ESM API、CLI 与 Schema exports                                                                                                        |
| `npm audit --audit-level=low`                                      | 退出 0；0 个已知漏洞                                                                                                                                                                 |
| `npm pack --dry-run --workspace @coga/factory --json`              | 退出 0；`@coga/factory@0.3.0`，73 个文件，72,624 B 压缩/377,795 B 解包；未生成可保留发布物、未发布                                                                                   |
| `npm run release:test`                                             | 退出 0；Core 发布候选回归 9/9，本轮未触发版本工作流或任何发布动作                                                                                                                    |
| `git diff --check`                                                 | 退出 0                                                                                                                                                                               |
| [实现 PR #7](https://github.com/kayz/coga-core/pull/7)             | Linux [run 31601722053](https://github.com/kayz/coga-core/actions/runs/31601722053) 5/5 通过并合并为 `main` 提交 `caf46c7f`                                                          |
| [Birch Draft PR #8](https://github.com/kayz/coga-core/pull/8)      | head `bff770b9`；Linux [run 31601924133](https://github.com/kayz/coga-core/actions/runs/31601924133) 5/5 通过                                                                        |
| [Cedar Draft PR #9](https://github.com/kayz/coga-core/pull/9)      | head `785d3a17`；Linux [run 31601956074](https://github.com/kayz/coga-core/actions/runs/31601956074) 5/5 通过                                                                        |
| [权限修复 PR #10](https://github.com/kayz/coga-core/pull/10)       | Linux [run 31603583876](https://github.com/kayz/coga-core/actions/runs/31603583876) 5/5 通过并合并为 `main` 提交 `be319f07`                                                          |
| [Birch v3 Draft PR #11](https://github.com/kayz/coga-core/pull/11) | head `56e18007`；Linux [run 31603794904](https://github.com/kayz/coga-core/actions/runs/31603794904) 5/5 通过                                                                        |
| [Cedar v3 Draft PR #12](https://github.com/kayz/coga-core/pull/12) | head `1d5f05ec`；Linux [run 31603823260](https://github.com/kayz/coga-core/actions/runs/31603823260) 5/5 通过                                                                        |

首次真实 `workflow_run` 在 [Birch run 31601993389](https://github.com/kayz/coga-core/actions/runs/31601993389) 与 [Cedar run 31602032117](https://github.com/kayz/coga-core/actions/runs/31602032117) 均 fail closed：job 显式权限未包含读取触发 run 所需的 `actions: read`。PR #10 显式补齐 `actions: read`、`pull-requests: read` 和诊断后，v3 工作流已成功读取 run、PR 与文件列表，但 [Birch run 31603871859](https://github.com/kayz/coga-core/actions/runs/31603871859) 和 [Cedar run 31603892338](https://github.com/kayz/coga-core/actions/runs/31603892338) 又在路径检查 fail closed：原 jq 字符类把转义后的 `\\u0000` 解释成普通排除字符，误拒绝安全路径。本修复改为逐 Unicode code point 拒绝控制字符、DEL 和反斜杠，并让无 Evidence Bundle 的普通 PR 无 attestation 地退出。Work Order 改用新的 v4 交付分支，以便修复合并后从最终 `main` 重新生成精确基线候选；真实 v4 run 结果只在交付 handoff 中报告，不在源代码候选中预写为通过。

0.4 合并后，真实 App 已从 `main@9e4202a` 创建 Birch Draft #17 与 Cedar Draft #18；两者 author 均由交付 REST API 证明为 `coga-factory-kayz[bot]`，五项检查和 Evidence attestation 均成功。首次 pre-approval collector 又 fail closed：`gh pr view` 的 GraphQL author 显示为 `app/coga-factory-kayz`，与 REST canonical login 不同。collector 因此统一改用 REST PR 快照并验证 base/head 仓库、ref、commit；Work Order 分支升为 v6，修复合并后从新 `main` 重建候选，不把旧基线 v5 计作完成证据。

Factory 0.4 经 PR 安全审查加固后的代码候选 `0af8235`，已取得 [实现 PR #16](https://github.com/kayz/coga-core/pull/16) 的公开 Linux [run 31611696852](https://github.com/kayz/coga-core/actions/runs/31611696852)：精确 head `0af8235f93202360276d4106992434f5b1f8f2a8`，`verify`、`factory-cell` 与 Node 20/22/24 安装消费共 5/5 成功，随后 squash 合并为 `main@9e4202a`；合并后 push run `31612043478` 同样成功。实现 PR 的作者是人类开发者 `kayz`，不作为 Factory 生成候选必须满足的 App author 证据。

同一代码候选的本地证据（Node.js `24.18.0` / npm `11.16.0`）：

| 命令或检查                                                       | 结果                                                                                                                                         |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| GitHub App 交付凭据定向测试                                      | 退出 0；12/12，覆盖新旧 installation token、继承环境、credential helper/hook/trace、URL rewrite、本地 branch/head、PR author/repository/ref  |
| `npm test --workspace @coga/factory`                             | 退出 0；38/38；Docker 专项 1 项按设计分离；恢复、重放和 planning 用例均从精确提交 `0af8235` 克隆验证                                         |
| `npm run check:public`                                           | 退出 0；Core 62/62、Factory 38/38、公开候选负例 13/13、release Profile、27 份 Markdown、210 个公开文本/候选的隐私与分类门禁通过              |
| `npm run factory:e2e`                                            | 退出 0；真实断网 Docker 从 `0af8235` 完成双目标影响、补丁、Core 校验、Application 测试/构建、Evidence Bundle 与本地候选                      |
| `npm run check:local`                                            | 退出 0；包含以上公开/Docker 门禁；私有 Application typecheck、40/40 测试、37 个产物（6 页面/2 组件）与 local Instance 校验通过               |
| `npm run package:consumer-test`                                  | 退出 0；在空目录安装 Core/Factory tarball，验证 Factory v0.3 protocol、机器身份公开 API、CLI 与六份 Schema export；Node 20/22 由远端矩阵复验 |
| 根项目与私有 Application `npm audit --audit-level=low`           | 两处均退出 0；0 个已知漏洞                                                                                                                   |
| `npm run release:test`                                           | 退出 0；9/9；Core 发布工具回归通过，未触发 tag、Release、attestation 或 npm 发布                                                             |
| `npm pack --dry-run --workspace @coga/factory --json`            | 退出 0；`@coga/factory@0.4.0`，77 个文件，81,114 B 压缩/416,805 B 解包；未生成可保留发布物、未发布                                           |
| `npm pack --dry-run --workspace @coga/core --json`               | 退出 0；`@coga/core@0.2.0`，60 个文件，55,714 B 压缩/277,293 B 解包；未生成可保留发布物、未发布                                              |
| Prettier、`npm run docs:check`、`git diff --check` 与 clean 状态 | 均退出 0；协议、示例回执、Work Order 摘要、README/知识模型/设计差异同步；工作区无未提交文件                                                  |

以上证明 0.4 实现已合并并通过本地与公开 Linux 门禁，但还不是 D-17 的完整机器交付身份证据。下一门槛是合并 REST author 修复，再从新 `main` 由真实 GitHub App 重建 Draft、由独立人类对精确 head 审批并收集 Remote Evidence。

## 5. 剩余风险

- Factory 0.4 的本地契约与负例不替代真实身份验收。0.3 阶段由 `kayz` 创建的 v4 Draft 证明了 CI/attestation/Patch 闭包，但作者与审批者未分离，不能计作 0.4 通过。必须在实现 PR 合并后创建/安装声明的 GitHub App，用其短期 installation token 生成新候选，再由 `kayz` 对精确 head 审批并由 collector 提升为 ready。
- GitHub App 创建、私钥托管、短期 token 签发、安装仓库范围、branch protection/ruleset 与 Environment 审批是远端控制面；代码只能 fail closed 地消费这些事实，不能用仓库内配置自证管理员授权。
- D-11 只完成静态部分：微信 DevTools 编译、模拟器、真机、屏幕阅读器和真实渲染对比度仍需人工证据；Node 构建不替代平台验收。
- 本地权益完整性标记只用于 deterministic fixture，不是生产密码学签名；真实后端身份、密钥轮换和失效机制不在本轮范围。
- 生产事故协同场景明确为 manual；没有伪造自动执行结果。
- D-12 的远端部分未执行：需先把候选合并为 `main` 精确 tip，再由受仓库规则授权的人创建签名 `v0.2.0` annotated tag。工作流最多产生 attestation 和 draft；管理员还须核验 signer、immutable release 设置并另行完成最终发布。工作流不会发布 npm。
- 当前阶段只推送开发分支并创建 draft PR；Linux `verify`、Factory Docker 和三档 package-consumer checks 已取得真实远端候选证据。PR 通过不等于授权合并、创建 tag 或发布。
- `@coga/core` 尚不存在于 npm registry；首次发布需要单独授权、命名空间权限和 2FA。完成首次 bootstrap 后，后续版本才可配置 OIDC trusted publisher。
- Core 验证证据记录，不验证审批人的外部授权，也不执行 evidence 指向的测试；受信 Runner 和人工治理仍是必要边界。
- D-15 的本地协议已关闭：Agent Proposal Receipt 已覆盖模型声明、提示、工具/预算、完整输入与规范化输出。示例回执不等于模型提供者密码学证明，也不声称 Factory 在线调用模型。
- D-16 的本地实现和测试已完成；v2 已证明缺权限时会 fail closed，v3 已证明安全路径误判时会 fail closed，修复后的 attestation 仍须由新的同仓库 v4 Draft PR 给出远端证据。Human Policy review 必须由授权人真实提交，不能由 Factory 或测试伪造。
- Factory Evidence Bundle 证明受控执行事实，不证明领域语义已获批准；示例 Policy 仍在 `pendingPolicies`，所以结果只能是 Draft。
- 受支持二进制只检查扩展名、文件头和大小，不能保证图片或 PDF 不含业务敏感内容。
- v0.1 使用者必须显式迁移；没有自动 visibility、版本、历史证据或审批推断。
- 本轮只允许当前开发分支 push、draft PR 与只读 Linux CI；不发布、打 tag、创建 GitHub Release、部署或操作 npm。

## 6. 变更面

0.2 已覆盖 Core、Schema、公开示例、私有 Application conformance 与发布候选工具；Factory 0.3 已覆盖提案、fan-out 和远端证据。Factory 0.4 增量涉及 `packages/factory/`、`.coga/work-orders/`、两份 Application Factory 定义、`factory-evidence.yml`、`CODEOWNERS`、消费测试以及现有 README、CHANGELOG、设计差异和本调整记录。私有 Application 继续由 `/private/` 忽略，不能进入公开提交。
