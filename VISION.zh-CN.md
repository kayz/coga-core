# COGA 愿景

**Cognition · Organization · Growth · Architecture**<br>
**认知 · 组织 · 生长 · 架构**

COGA 是一个用于培育“扎根于领域的 AI 原生软件工厂”的开源基础。

它的目标不是根据一次提示生成一个 Application，而是**标定一个有界领域**，使一个受到治理的 AI 组织能够在该领域中反复生产、验证、交付、运行、修复和演进多个 Application。

## 软件资产正在上移

当 Application 代码越来越容易生成，真正稀缺的资产会向上移动：

- 对领域的精确定义；
- 用于区分“正确”与“看起来合理”的领域规则；
- Agent 可以安全调用的能力和契约；
- 能够证明结果的场景与评测器；
- 让软件长期存活的交付与运行知识；
- 告诉机器何时行动、何时停止的治理体系。

COGA 将这些资产显式化、版本化、尽可能变成可执行对象，并保持它们与人类权威来源之间的可追溯性。Application 仍然重要，但它不再是领域知识唯一的容器，而是持久化 **Domain Harness** 的消费者。

## 三个边界

### COGA Core：开源的通用机制

COGA Core 不包含领域知识。它定义以下工厂协议和控制面机制：

- 资产 Schema、生命周期、来源和发布；
- Task Contract、证据、策略、审批和审计；
- Harness Package、依赖锁定、目录和影响分析；
- Agent、工具、工作区和验证器适配接口；
- 可重复的 Application 生产与运行闭环。

Core 知道工厂如何工作，但不知道券商客户、医疗诊疗或制造订单意味着什么。

### COGA Instance：扎根领域的工厂

COGA Instance 将 Core 与一个有界领域、一个真实组织、交付平台、工具环境和运行知识结合起来。它需要回答：

- 这个领域里存在哪些对象？
- 什么必须始终成立？
- 可以使用哪些能力？
- 哪些来源具有权威性？
- 怎样证明结果正确？
- 哪些变化必须由人类判断？
- 一项领域变化会影响哪些 Application？
- Application 怎样被观察、修复和升级？

一个 Instance 应当能够持续维护多个 Application。如果它只能解释一个产品，就还没有证明领域知识已经真正从 Application 上移。

### Application：独立有用的产品

Application 拥有自己的用户问题、用户旅程、界面、产品策略、代码、应用专属场景和运行目标。它声明对 Instance Harness Package 的精确版本依赖，并且可以独立构建和部署。

Application 的正常用户请求不应依赖工厂控制面实时在线。COGA 负责生产和维护 Application，而不是被偷偷插进每一次业务请求的调用链。

## 工厂生长闭环

```text
权威来源与人类意图
        ↓
Agent 辅助领域标定
        ↓
人类审查语义与影响范围
        ↓
版本化 Domain / Platform / Operations Harness
        ↓
Application 生产闭环
        ↓
测试 · 证据 · 预览 · 发布决策
        ↓
运行信号与事故
        ↓
候选场景、规则和能力
        └────────────────────────↺
```

系统通过“升格”生长，而不是依靠未经审查的自动记忆。运行观察首先是局部证据；只有经过泛化、验证、来源登记和相应风险级别的批准，才能成为共享资产。

## 人类治理

COGA 不把“无人”理解为“无人负责”，而追求**有人治理、无人值守执行**：

- 人类负责目标、领域语义、例外、风险接受和不可逆的生产决策；
- Agent 负责研究、建模、实现、测试、诊断和准备证据；
- 确定性系统负责 Schema、契约、质量门、权限、制品来源和回滚条件。

因此，人类操作界面应当是治理工作台，而不是聊天记录。未来的 COGA Workbench 将呈现候选资产、结构化表单、语义 Diff、来源、影响图、验证证据和审批动作；底层版本化文件仍然是事实来源。

## 开放与私有共同生长

COGA Core 是开源项目。可复用的公共领域包和平台包也可以开源。真实 Instance 可以将这些公共资产与组织私有策略、受限来源、Git 外部管理的凭据以及机密 Application 知识组合起来。

公开发布必须使用白名单：只有被明确标记为公开，且其依赖和来源的传递闭包也全部公开的 Core 与 Instance 资产，才能进入开源发布物。私有 Application 不能成为公开资产的隐式来源。

## 0.2 已证明什么

0.2 规模仍然有限，但已经证明：

1. Core 能验证并生成版本化 Harness 资产目录。
2. 一个有界的示例 Instance 能表达领域、平台、工程、组织和运行知识，同时不嵌入某一个具体产品。
3. 多个 Application Manifest 能消费 Instance Package 的精确版本。
4. 一项领域变化能够生成确定性的 Application 影响报告。
5. 一个真实的本地私有 Application 能消费相同契约，同时不向公开发布物泄漏私有材料。
6. 最终公开候选可以复现、通过本地自测并安全克隆。
7. 一项受治理、精确版本的 Artifact 变化可以映射到全部受影响 Application，经受限补丁与登记的隔离适配器验证，形成内容寻址证据，并且在没有人类批准时最多交付为 Draft PR。

Factory 仍接受外部准备的 Agent Patch，而不在线调用模型；当前会把模型、上下文、工具声明和精确输入绑定为可重现回执，回收精确远端 CI 证据，并通过持久 Operations 队列运行有界任务。模型提供者签名的真实执行与目标平台验收仍属于外部证据，不能被暗示为已有保证。

## COGA 不是什么

COGA 不是通用本体，不是通用规则语言，不替代 GitHub 或 CI，不替代基础设施编排器，不为 LLM 提供生产 Shell，也不承诺所有软件工作都能自动化。它把已有工具组合在明确契约之后，并使领域化反馈闭环可以被治理。

## 完整企图

长期检验标准不是 Agent 能否生成第一个 Application，而是：当同一领域发生第 5 次、第 20 次变化时，它们是否因为工厂积累了受控知识，而变得更快、更安全、更容易解释、更少依赖人的具体执行。

当这种能力在多个 Application 中成立时，Domain Harness 就不再是文档，而成为能够复利增长的软件生产资产。
