# Access rule scenario evidence

The deterministic fixture scenario confirms that a server-issued entitlement enables only its
matching capability and that the client does not elevate access locally.
