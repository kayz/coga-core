# COGA 设计方案及差异

> 文档状态：Core 0.2 / Factory 0.4 身份分离与真实治理门禁候选
>
> 审计日期：2026-08-12
>
> 实施基线：`main@3a41e1191883278c529409426142b3ab73982632`
>
> 覆盖范围：公开仓库、公开示例、Factory Cell，以及被 Git 忽略的本地 Application 一致性检查

## 1. 结论

项目仍遵守文件优先、Schema 驱动、Core 与领域知识解耦、Instance 复用 Harness、Application 精确锁定版本的核心方向。0.2 语义闭包已关闭 D-01 至 D-08；本次后续候选又以本地可执行证据关闭 D-09、D-10，完成 D-11 的静态部分，并为 D-12 建立 LICENSE、可复现发布载荷、SBOM、摘要和最小权限的 attest/draft 候选门禁。

当前能力分为四层：

1. **结构与语义闭包层（0.2 已实现）**：有预算地装载四类资源，校验 Schema、契约、精确资源图、生命周期/Scope、公开传递闭包和版本化影响路径。
2. **证据记录层（0.2 已实现）**：验证 completed validation 的时间、验证器与证据，以及 `approvalRules` 所要求的精确 Policy attestation；Core 不伪造或执行这些证据。
3. **Application 运行证明层（本地部分已实现）**：私有 fixture Application 已提供权益、遥测、状态、页面与场景测试；真实后端、微信 DevTools/模拟器/真机、生产事故演练和审批人的外部权限仍不在自动保证内。
4. **Factory Cell（0.4 身份门禁候选）**：版本化 Work Order 和 Agent Proposal Receipt 绑定精确 Git 基线、Artifact、模型/提示/工具/预算、Harness 输入闭包、规范化输出、Policy 和完整影响目标；控制器按 Application 隔离 worktree、状态、证据、提交与 Draft PR，并要求 GitHub App 机器交付身份与精确 head 人类审批身份分离。最强自动动作仍是 Draft-to-ready-for-review。

`coga.dev/v0.2` 是破坏性 envelope，明确拒绝 v0.1；没有双读、自动猜测 visibility、版本、历史证据或审批结果。本地 Application 仍是被 Git 忽略、禁止发布的 fixture-only 验证对象；其自动化证据不能替代真实平台和生产验收。

## 2. 审计依据与边界

### 2.1 检查对象

- Core：`packages/core/src/`、`packages/core/schemas/`、CLI、单元测试和发布包内容。
- Instance：`examples/broker-digital-channel/` 的 6 个 Harness Package、32 个 DomainArtifact 和 2 个虚构 Application manifest。
- Factory：`packages/factory/`、`.coga/work-orders/`、真实 Cedar H5 参考 Application、Docker 端到端测试与 Draft PR 适配器。
- 本地 Application：`private/application/` 的 manifest、领域模型、适配器、页面、组件、构建脚本和测试；该目录不属于公开发布物。
- 工程门禁：根目录脚本、`public.release.json`、Git 忽略规则、GitHub Actions 和仓库保护设置。
- 文档：README、愿景、知识模型、治理流程和 Agent 操作手册。

### 2.2 审计基线证据

| 检查                                    | 结果                                                                                                          |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `npm run check:local`                   | 通过；Core 11 项测试、本地 Application 25 项测试通过，公开示例、文档链接、隐私和边界检查通过                  |
| Node.js 20 兼容性复核                   | `@coga/core` 的 11 项测试与 CLI 示例校验通过                                                                  |
| 根项目及本地 Application 的 `npm audit` | 生产与完整依赖均为 0 个已知漏洞                                                                               |
| 当前 Schema/实例校验                    | 4 类资源、5 个 Package、30 个 Artifact、2 个公开示例 Application 均可装载并通过当前规则                       |
| 发布包预览                              | `@coga/core` 可打包，预览包含 47 个文件；包内缺少 `LICENSE`                                                   |
| 公开边界与历史扫描                      | 当前工作树和 Git 历史未发现已知私有标识、密钥模式或 `private/` 提交记录                                       |
| 远端治理快照                            | HEAD 工作流通过；`main` 要求 `verify`、禁止强推/删除；Secret Scanning 与 Push Protection 开启；未配置代码扫描 |

这些结果属于 0.1 审计基线。0.2 最终候选的实际命令、退出码和测试数量统一记录在 [`本次项目调整.md`](本次项目调整.md)，不得用本表替代最终回归证据。

### 2.3 本次未声称完成的验证

- 未连接真实后端、生产数据或生产密钥。
- 未用微信开发者工具完成真实预览、真机或平台审核。
- 未执行发布、部署、npm 发布或目标环境操作。
- 未将本地 Application 视为生产就绪；其 manifest 明确属于 fixture-only、禁止发布的验证对象。

## 3. 当前架构与事实边界

| 层            | 当前事实来源                                             | 0.2 当前已保证                                                                                            | 不属于当前保证                                 |
| ------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| COGA Core     | TypeScript、JSON Schema、测试                            | 安全装载、Schema/契约/资源图/Profile、证据记录、目录和版本化影响路径                                      | 领域含义、任意命令执行、外部审批人的权限真实性 |
| COGA Instance | `instance.yaml`、Package 与 Artifact 文件                | 有界领域、精确资源闭包、Policy 规则与发布记录                                                             | 自动证明每条规则已被 Application 运行时执行    |
| Application   | Application manifest 与实现代码                          | 交付目标、精确 Harness/契约/Scenario/Runbook 锁和产品选择                                                 | 重新定义共享规则或把声明自动变成运行时证据     |
| Factory Cell  | Work Order、Proposal Receipt、Application Factory 与 Git | 精确影响 fan-out、提案输入/输出归因、受限变更、隔离验证、本地/远端证据、Draft PR 与 ready-for-review 门禁 | 自动审批、合并、发布、部署或通用工作流编排     |

规范优先级如下：

1. 对“当前行为”的描述，以可执行代码、Schema 和通过的测试为准。
2. README 和操作手册必须准确解释当前行为及限制。
3. 愿景、治理流程和本文件的目标设计描述未来状态，不可作为当前自动化保证。
4. 任何把目标升级为保证的改动，都必须同时提交实现、测试、迁移说明和文档。

## 4. 设计差异清单

优先级保留审计时定义；状态以当前 0.2 候选为准。

| ID   | 优先级 | 设计意图                                     | 0.2 状态 | 当前对齐结果或剩余工作                                                                                                                                               |
| ---- | ------ | -------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01 | P0     | 公开候选文件均被分类                         | 已实现   | boundary/privacy 共用 Git NUL 候选与分类；文本、受支持二进制、总量均有预算，未知类型和 ZIP 失败；二进制业务内容仍需人工检查                                          |
| D-02 | P0     | Canonical resource 不携带明文秘密            | 已实现   | 敏感上下文向子节点继承，键名规范化并覆盖 authorization、credential 等；外部 secret reference 保留                                                                    |
| D-03 | P0     | 装载不可信本地输入时失败可控                 | 已实现   | Canonical/Contract 大小、深度、节点和 alias 有固定预算；循环和特殊对象返回稳定 `load.*` issue                                                                        |
| D-04 | P1     | Contract 引用可复现、可验证                  | 已实现   | 精确 id/version/path/format，校验 Draft 2020-12/OpenAPI 3.1、`x-coga-contract`、本地 `$ref` 与 release digest                                                        |
| D-05 | P1     | 发布由 Policy 与证据记录约束                 | 已实现   | Core 验证 completed validation、release evidence 与 approval attestation；Scenario/测试/审批决定按既定边界由外部受信系统执行                                         |
| D-06 | P1     | 影响分析返回直接、传递、旧锁消费者和路径     | 已实现   | `impact()` 接受精确 Artifact，返回去重 Application、全部版本化路径及 Scenario/Runbook 重跑集                                                                         |
| D-07 | P1     | 资源图形成合法闭包                           | 已实现   | 校验 Package DAG、Scope/生命周期、跨 Package relation 依赖路径、Policy 类型和 Application 绑定可达性                                                                 |
| D-08 | P1     | visibility 与公开发布形成强边界              | 已实现   | 所有资源显式 visibility；public/release 要求根目录与传递 public 闭包，local 允许明确的根外本地 Harness                                                               |
| D-09 | P1     | 本地 Application 以证据证明关键 Harness 规则 | 已实现   | 权益缺省拒绝、遥测双白名单、七态视图和 6 个 manifest 场景均有可执行测试或显式 manual 原因                                                                            |
| D-10 | P1     | 本地 Application fixture 行为与产品语义一致  | 已实现   | 两条研究详情均可打开；每日投票按主体/洞察/自然日持久化，并覆盖重启、跨日与损坏存储                                                                                   |
| D-11 | P1     | 小程序构建与无障碍检查覆盖真实页面           | 部分实现 | 固定基础库、37 个完整产物、ARIA 与静态色彩对比已验证；DevTools 编译、模拟器、真机和屏幕阅读器仍需人工证据                                                            |
| D-12 | P2     | 发布物可独立复用与追溯                       | 部分实现 | LICENSE、可复现 tarball、空目录消费矩阵、CycloneDX、SHA-256 与签名 tag/attest/draft 候选工作流已就绪；最终发布与 npm 首发未授权                                      |
| D-13 | P2     | 文档准确描述当前能力                         | 已实现   | README、知识模型、治理文档和 Agent 手册已迁移到 0.2 能力边界                                                                                                         |
| D-14 | P1     | 领域变化可稳定转换为真实 Application 候选    | 已实现   | 同一 Artifact 精确影响 Cedar/Birch 两个 H5 Application；每个目标独立计划、工作区、状态、证据、提交和 Draft PR，失败隔离且只重试失败项                                |
| D-15 | P1     | Agent 提案自身可重现、可归因                 | 已实现   | `AgentProposalReceipt` 绑定模型、提示摘要、工具/网络/文件系统策略、预算、完整 Harness 输入摘要、Patch 规范化算法和授权输出；示例不声称由 Factory 在线调用模型        |
| D-16 | P1     | 远端运行证据与人工 Policy 能安全提升候选     | 部分实现 | `RemoteEvidence`、治理视图和 Draft-to-ready 门禁已实现；v4 已证明 5 项 Linux check、attestation 和 Patch 闭包，但仍缺真实 Human Policy approval                      |
| D-17 | P1     | 机器交付身份与人类审批身份必须分离           | 本地实现 | Factory v0.3 protocol、App installation preflight、PR author 绑定、身份互斥、CODEOWNERS/check 对齐和负例已实现；仍须真实 App 候选与人类精确 head review 完成远端验收 |

当前没有发现已提交的私有内容或已知依赖漏洞。所有“已实现”状态必须以 `本次项目调整.md` 中最终同一候选回归结果为准，不等于真实 Application、外部审批权限或二进制业务内容已获得合规证明。

## 5. 0.2 已实现设计

### 5.1 三档校验 Profile

一个 API 提供三个明确的校验档位：

| Profile   | 用途                | 行为                                                                                |
| --------- | ------------------- | ----------------------------------------------------------------------------------- |
| `local`   | 本地可信仓库        | 0.2 安全预算、Schema、契约、图闭包和秘密检查；允许根目录外的显式本地 Harness        |
| `public`  | 公开候选            | 要求显式 public visibility、根目录内路径、传递公开闭包、严格文件类型/大小和秘密扫描 |
| `release` | 不可变 Harness 发布 | 包含 public 全部规则，并要求契约、验证/发布证据、审批引用、版本与摘要完整           |

CLI 形式为 `coga validate <instance> --profile local|public|release`，参数顺序无关。库 API 使用显式 options；已装载对象保存 Profile、rootDir 和 limits，复用时传入不同 options 会被拒绝。

### 5.2 安全装载与公开边界

1. 装载前检查文件真实路径、类型和大小；public/release Profile 禁止逃逸 Instance 根目录。
2. YAML 解析后转换为有预算的无环 JSON-like 图；拒绝函数、特殊原型、自引用和超限别名。
3. 所有递归检查共享访问集合与最大深度，错误返回稳定 issue code，不抛出未捕获 `RangeError`。
4. 发布脚本先枚举所有候选文件，再应用精确文件或目录策略；未分类扩展名默认失败。
5. 文本统一按受控编码扫描；允许的二进制逐项登记来源、用途、摘要和人工检查证据。
6. 秘密检测同时考虑当前键、祖先键和内容模式；豁免必须绑定文件、规则、原因和到期时间。

### 5.3 版本化资源图

所有节点使用 `(kind, id, version)` 作为精确身份。至少建立以下有向边：

- Instance → Package、Instance → Application；
- Package → dependency Package、Package → owned Artifact；
- Application → locked Package、Application → Scenario/Runbook；
- Artifact → relation Artifact、Artifact → Scenario、Artifact → Contract；
- Governance → Policy。

图校验必须回答：目标是否存在且类型正确、版本是否精确、Scope 是否允许、源是否能通过其 Package 闭包到达目标、生命周期是否允许发布、是否存在禁止的环。错误输出必须包含源文件、JSON Pointer、目标身份和稳定 issue code。

### 5.4 契约注册与校验

0.2 的 `contractRef` 要求：

- `id`、`version`、`path`；
- 可辨识的 `format`（OpenAPI 3.1 或 JSON Schema 2020-12）；
- release Profile 下的内容摘要；
- 文件身份与引用身份一致，且路径位于允许根目录；
- OpenAPI/JSON Schema 自身通过对应规范校验。

为了避免 Core 猜测任意文件的业务语义，契约是否满足领域行为仍由场景或 Application 契约测试证明。Core 只负责身份、格式、闭包和证据引用。

### 5.5 生命周期、证据与审批

`validation` 不只保存 `passed` 字样。`passed/failed` 记录要求：

- `checkedAt`、稳定的 `validator` 身份和至少一条 `evidence`；
- 证据引用不可变提交、CI run、测试报告或签名摘要；
- Scenario 的 Given/When/Then 声明与 Runner 结果分离；
- `governance.approvalRules` 的精确 Policy 均能解析，受控 lifecycle 资源携带匹配 attestation；
- `governance.releaseEvidence` 为每个受控 lifecycle 保存至少一个非空发布证据定位符；
- 证据过期规则由 Policy 声明，不在 Core 中硬编码某个行业周期。

Core 不直接执行 Artifact 中的任意命令。受控 Runner 只运行仓库预先登记的适配器，避免把数据文件变成代码执行入口。

### 5.6 传递影响分析

当前输出包含：

```text
changed artifact@version
  -> owning package@version
  -> dependent package@version (0..n)
  -> application@version
```

每个结果标记 `direct`、`transitive` 或 `older-pin`，保留完整路径，并列出需要重跑的 Scenario/Runbook。纯图结果由 Core 确定性生成；“权限扩大、失败语义改变”等语义判断仍由 Human/Agent 基于 diff 给出，不能与图可达性混为一项自动保证。

### 5.7 Application 证明接口

Harness 是规范，Application 必须提供实现证据。建议本地 Application 采用以下适配器边界：

- 权益：返回 `valid | expired | malformed | contradictory | unavailable` 的判定结果；在进入领域模型前验证发行者、受众、有效期和完整性证据。
- 遥测：统一信封强制 owner、purpose、schemaVersion、retention、classification，并对事件名和属性使用双重 allowlist。
- 页面状态：使用含 `loading | empty | ready | stale | denied | recoverable-error | terminal-error` 的穷尽联合类型；只有 recoverable error 提供重试。
- 场景证据：每个 Application manifest 场景绑定一个测试 ID 和最近通过的证据；无法执行的场景标为 manual，而不是预填 passed。
- 小程序交付：固定平台基础库，检查完整页面/组件产物，并把开发者工具编译、预览和真机结果作为不同证据，不互相替代。

### 5.8 Factory 0.3 证据闭环（已完成基线）

Factory 执行层与 Core 校验层分离。Core 继续只验证资源和已记录证据；`@coga/factory@0.3.0` 只接受 `coga.dev/factory/v0.2` 的封闭协议：

1. Work Order 必须来自精确 Git 提交，绑定变更 Artifact、domain patch、每个 Application 的 Proposal Receipt、允许路径、全部受影响 Application、Policy 和独立 Draft PR 目标。
2. 控制器重新执行 Core validation 与版本化 impact；Work Order 少报、多报或重复 Application 均失败。
3. Application 自己登记可修改路径和 `coga.node.test/v1` / `coga.node.build/v1` 适配器；Work Order 不允许携带 shell 命令。
4. 补丁禁止路径逃逸、删除、重命名、复制、symlink 和 submodule；中断恢复状态必须等于从当前 Work Order 重新派生的完整目标 Plan，并绑定工作区、分支、步骤前缀和 Git worktree 身份。
5. Node 适配器固定到镜像摘要，使用断网、只读根/仓库、非 root、无 capability、no-new-privileges 和资源预算的 Docker；构建只写临时输出挂载。
6. Agent Proposal Receipt 绑定声明的模型/提供者/版本、提示模板及摘要、工具/网络/只读文件系统策略、预算、完整 Harness 输入文件摘要、规范化 Patch 摘要和授权输出路径；模型不获得 Git 写权限。
7. 每个 Application 使用不同 worktree、分支、恢复状态、Evidence Bundle、提交和 Draft PR。一个目标失败只产生 `partial` 聚合结果；重跑复用已完成目标。
8. Evidence Bundle 绑定 base commit、subject tree、Work Order/Plan、Application、Proposal Receipt、每个变化文件、impact、审批/待审批分区、适配器回执和沙箱事实，并以自身 SHA-256 寻址。
9. GitHub 适配器只创建或复用 open Draft PR；默认分支上的 `workflow_run` job 不 checkout、不执行仓库代码，只对成功的同仓库 Draft PR 精确 Evidence Bundle 创建 attestation。
10. Remote Evidence 从精确提交复核 Work Order，重新下载远端 domain/proposal Patch 并要求其完整路径闭包等于 Bundle/PR 文件集，再绑定 PR base/head、全部必需 checks、artifact attestation 与授权审阅者在精确 head 上的 Policy marker；最终 TOCTOU 复核通过后，最强动作仅为 Draft-to-ready-for-review。

参考 Work Order 修改 `web.h5.responsive.shell@0.2.0`，影响分析精确返回 `application.cedar.insight.h5@0.2.0` 与 `application.birch.insight.h5@0.2.0`。两者独立产出候选；领域审批仍为空，因此本地 Evidence Bundle 明确保留 pending Policy，Draft 状态不是批准。

### 5.9 Factory 0.4 身份分离

Factory protocol 升级为 `coga.dev/factory/v0.3`，只提供 `github.app-draft-pr/v3` 远端交付：

1. Work Order 固定 GitHub App slug 和唯一凭据入口 `COGA_FACTORY_GITHUB_TOKEN`，不允许资源文件选择任意 secret 名称。
2. 在任何 push 前，适配器使用 installation 专用 API 枚举有预算的仓库集合，并要求精确目标仓库存在；用户 token、错误安装和不完整分页均失败。
3. Installation token 只进入清洗后的子进程环境；Git 使用无 token 的 HTTPS URL 和临时 extraheader，`gh` 使用独立 `GH_TOKEN`。参数、remote URL、Work Order、Evidence 和错误信息均不得泄漏 token。
4. 创建或复用后必须验证 PR author 精确为 `<app-slug>[bot]`；Remote Evidence 保存该作者，collector 在采集、提升前和提升后三次绑定 author/base/head/file-count。
5. App slug 和 bot login 不能成为授权审批者。只有允许名单中的人类 `APPROVED` review、精确 Policy marker 和当前 head commit 才满足审批。
6. `CODEOWNERS` 固定人类所有者；Work Order 的五项 checks 与公开 CI job/matrix 由单测对齐。远端 branch protection 还须要求 1 个 CODEOWNER review、dismiss stale reviews 和 last-push approval。
7. `--promote` 仍只允许 open Draft 变为 ready；没有 merge、tag、Release、npm publish 或 deploy 路径。

## 6. 兼容与迁移方案

| 对象                    | 0.2 迁移决策                                                                                               |
| ----------------------- | ---------------------------------------------------------------------------------------------------------- |
| v0.1 canonical resource | 明确拒绝，不双读；调用方必须显式迁移到 `coga.dev/v0.2`                                                     |
| metadata                | scope、visibility、attestations 全部必填，不推断可见性或审批                                               |
| 精确引用                | Artifact relation、Validation target、Policy、Contract 全部携带版本                                        |
| Contract                | 必填 format 与 `x-coga-contract`；release 引用必须保存最终原始字节的 SHA-256                               |
| 历史 validation         | 公开示例中 60 条无可追溯证据的 `passed` 降为 `pending`，相关 19 个 Artifact 从 `published` 降为 `approved` |
| Core valid fixture      | 3 条 `passed` 保留并补真实本地证据、验证器/时间与 Policy attestation                                       |
| 本地 Application        | 在 manifest/精确锁基础上补权益、遥测、七态、fixture、投票持久化、静态交付和场景证据；仍禁止公开或发布      |

早期审计记录中的“62 条”来自混合盘点估计。最终 tracked 实盘是公开示例 60 条无证据记录，加 Core 有效 fixture 3 条可补证据记录；本轮按 `60 + 3` 处理，不制造两条不存在的迁移。

## 7. 实施状态与后续验收

### 阶段 A：0.2 安全门禁（本轮已实现）

- 修复循环对象崩溃和嵌套敏感字段绕过。
- 公开候选的每个文件都有分类结果，未知类型失败。
- 增加针对 CSV、PDF、图片、压缩包、路径逃逸、循环 YAML、深层对象和嵌套秘密的负向测试。
- 验收：有效 fixture 通过；所有构造绕过变成稳定的 issue；进程不崩溃。

### 阶段 B：0.2 语义闭包（本轮已实现）

- 实现契约注册、版本化资源图、Scope/生命周期矩阵和传递影响。
- Core valid fixture 使用真实 OpenAPI 3.1 契约、摘要、验证证据和审批 attestation。
- 验收：悬空 Policy、Package 环、不可达 Runbook、published Package 包含 candidate Artifact、旧版本消费者均有确定结果和快照测试。

### 阶段 C：Application conformance（本地自动化已完成）

- 重构权益、遥测、状态和 fixture；建立 manifest 场景到测试证据的映射。
- 完成小程序完整构建、ARIA 和颜色对比度静态检查；开发者工具 CLI 无法给出可信无交互结果，保留人工检查。
- 验收：过期/伪造/畸形权益默认拒绝且不抛异常；遥测不能记录未登记字段；第二条研究内容可打开；每日投票跨日期与重启行为有测试；所有声明场景有证据或明确标为 manual。

### 阶段 D：发布成熟度（本地准备已完成，远端受门禁）

- `LICENSE` 已纳入 npm 包；正式 npm 发布决定为本轮不执行。
- 本地生成规范化 CycloneDX 1.5、SHA-256 发布清单和字节可复现载荷；版本工作流要求 GitHub 验证的 annotated 签名 tag 精确指向 `main` tip。只读 job 构建并校验资产，高权限 job 不 checkout 或执行仓库代码，只消费摘要绑定的 handoff，生成 GitHub artifact attestations 和 draft 候选。
- 普通 PR CI 从干净构建生成 Core 与 Factory `.tgz`、同时安装到空目录，再通过安装后的包验证两套 ESM API、CLI 与 Schema exports；Linux 矩阵精确覆盖 Node.js 20.20.2、22.22.1、24.18.0。该步骤只证明开发者预览候选，不触发版本工作流。
- 工作流不会自动公开可变 Release；管理员必须在远端确认 tag signer 授权、仓库 immutable release 设置和候选证据后，另行完成最终发布。
- `@coga/core` 当前在 npm registry 不存在。npm 官方要求包已存在后才能配置 trusted publisher，因此首次包名占用/发布必须由人类另行授权并以 2FA bootstrap；本候选不保存 token，也不声称自动首发可用。
- 验收：干净提交可重复生成相同三件发布资产；draft PR 的公开 Linux CI 与三档真实消费测试通过。版本 tag、attestation、GitHub Release 和 npm 仍需后续独立授权。

### 阶段 E：最小 Factory Cell（本轮已实现）

- 新增 Work Order、Application Factory 和 Evidence Bundle Schema，以及确定性计划、恢复状态、适配器清单和 Draft PR 交付。
- 新增真实 Cedar H5 参考 Application；一项受治理的 responsive-shell 变化能确定性生成其源码/测试变更。
- 单测覆盖命令注入、路径逃逸、删除、symlink、状态/计划/证据篡改和中断恢复；Docker 端到端验证真实断网测试/构建与沙箱回执。
- Core 与 Factory tarball 同时安装到空目录，并验证两者的 ESM API、CLI 与 Schema exports。
- 实现 Draft PR 与 Factory 生成的 Application Draft PR 已在公开 Linux CI 各通过 verify、Factory Docker 和 Node 20/22/24 消费共 5 项检查；生成 PR 相对 Factory 基线只包含 6 个授权文件，仍因待审批 Policy 保持 Draft。

### 阶段 F：0.3 提案编译与远端证据闭环（本地实现完成，真实远端验证中）

- 已定义只输出候选 Patch 的 Agent Proposal Receipt，固定模型、输入 Harness 闭包、工具权限、预算、提示模板摘要和输出归一化算法；编译命令只记录已有候选，不在线调用模型。
- 已将多 Application 影响拆成可重放 fan-out 计划，并为每个目标生成独立状态、证据、提交、失败隔离结果和 Draft PR。
- 已把远端 Linux CI check run、artifact attestation 和 Human Policy 审批定义为追加式证据；只有全部绑定精确 PR head 才允许 Draft 进入 ready-for-review，且仍不自动 merge/deploy。
- 已提供 JSON/Markdown 治理视图，展示每个目标的提案、本地/远端证据、待审批 Policy 和提升阻塞，不建立第二套权威数据库。
- 0.3 合并后，双 Application v2/v3 Draft PR 的 Linux CI 均 5/5 通过；v2 `workflow_run` 因缺少 `actions: read` fail closed，v3 已越过权限/API 绑定并在误转义的路径字符类处 fail closed。当前修复以 code point 明确拒绝 ASCII 控制字符、DEL 和反斜杠，跳过无 Bundle 的普通 PR，并由新 v4 分支从最终基线复验。Human Policy review 必须由授权人实际提交，不能由测试或 Factory 伪造。

### 阶段 G：0.4 机器交付与人类审批分离（本地实现，远端待验收）

- 本地协议、App token preflight、PR author 绑定、Remote Evidence author、身份互斥和负例完成后，先通过普通 Draft PR 的 Linux CI 合并实现。
- 合并后的最终 `main` 才能成为新 Work Order 基线；旧 v4 候选由人类账号创建，不能复用为 0.4 身份证据。
- 远端管理员创建并仅安装到目标仓库的 GitHub App，配置短期 token 签发、CODEOWNER/last-push review 和五项 required checks；私钥不进入仓库。
- App 创建 v5 Draft 后，由 `kayz` 在精确 head 提交带 Policy marker 的 APPROVED review；collector 在 checks/attestation 全部通过后生成 Remote Evidence，并仅执行 Draft-to-ready。
- 验收以 PR author、reviewer、head、checks、attestation、Remote Evidence digest 和最终 `isDraft=false/state=OPEN` 的同一候选快照为准；PR 仍不合并。

## 8. 非目标

- Core 不解释证券、微信、组织或产品业务规则。
- 不把 YAML 变成任意代码执行或通用 Policy DSL。
- 不引入第二套权威数据库；索引、图和表单仍是派生视图。
- 不把 Agent 判断当作审批、法律意见或运行证据。
- Factory 不给模型 Git 写权限；示例记录已有模型候选而不声称在线调用。测试或 attestation 通过不等于领域审批，也不自动 merge、publish、tag、release 或 deploy。
- GitHub App 私钥和 installation token 不进入 Work Order、Evidence、模型或沙箱；Factory 不创建 App、不扩大安装仓库范围、不修改管理员治理规则，也不自动合并其候选。
- 不伪造缺失版本、历史证据或审批，不为兼容而接受 v0.1 envelope。

## 9. 设计完成定义

后续能力只有同时满足以下条件，才可以从“目标”移入 README 的“已包含”：

1. Schema/API 行为有版本化定义和兼容说明。
2. 正向、负向、边界和回归测试在受支持 Node.js 版本通过。
3. CLI、库 API、README、知识模型和 Agent 手册使用同一能力边界。
4. 公开边界、隐私和许可证检查覆盖新增文件类型。
5. Application 层声明附有可重放证据，人工检查项明确标识为人工。
6. 从干净 checkout 可以复现校验结果与发布候选。
7. 涉及治理身份的能力必须有真实远端主体证据；本地 mock、提交 author 字段或同一人自审都不能替代机器 PR author 与独立人类 exact-head review。
