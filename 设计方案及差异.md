# COGA 设计方案及差异

> 文档状态：审计结论与后续设计基线  
> 审计日期：2026-08-12  
> 事实基线：`main` / `f7c736d82c31d098d1573e7892ffa27b39bcf3a1`（`v0.1.0`）  
> 覆盖范围：公开仓库、公开示例，以及被 Git 忽略的本地 Application 一致性检查

## 1. 结论

项目没有偏离最核心的架构方向：文件优先、Schema 驱动、Core 与领域知识解耦、Instance 复用 Harness、Application 精确锁定版本，这些边界已经同时体现在目录、Schema、类型、装载器、示例和测试中。

主要偏离发生在**能力成熟度的表述**上。部分文档把目标治理流程写成了 Core 0.1 已自动保证的能力，而当前实现更准确的定位是：

1. **结构层（当前已实现）**：装载 YAML/JSON、校验四类资源 Schema、检查一部分精确引用与发布条件、生成目录，并计算“资产所属 Package 的直接 Application 消费者”。
2. **语义闭包层（尚未实现）**：校验契约文件、依赖闭包、Scope 合法性、治理策略引用、版本化传递影响和完整公开边界。
3. **证据执行层（尚未实现）**：执行场景或测试、验证审批证据、强制来源新鲜度，以及把 Harness 规则证明到具体 Application 运行时。

本次对齐选择当前代码作为 0.1 的事实基线，把更强的设计承诺保留为 0.1.1/0.2 的显式实施方案。没有在文档调整中静默改变公共 Schema、CLI 或运行时语义。

## 2. 审计依据与边界

### 2.1 检查对象

- Core：`packages/core/src/`、`packages/core/schemas/`、CLI、单元测试和发布包内容。
- Instance：`examples/broker-digital-channel/` 的 5 个 Harness Package、30 个 DomainArtifact 和 2 个虚构 Application。
- 本地 Application：`private/application/` 的 manifest、领域模型、适配器、页面、组件、构建脚本和测试；该目录不属于公开发布物。
- 工程门禁：根目录脚本、`public.release.json`、Git 忽略规则、GitHub Actions 和仓库保护设置。
- 文档：README、愿景、知识模型、治理流程和 Agent 操作手册。

### 2.2 已取得的证据

| 检查                                    | 结果                                                                                                          |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `npm run check:local`                   | 通过；Core 11 项测试、本地 Application 25 项测试通过，公开示例、文档链接、隐私和边界检查通过                  |
| Node.js 20 兼容性复核                   | `@coga/core` 的 11 项测试与 CLI 示例校验通过                                                                  |
| 根项目及本地 Application 的 `npm audit` | 生产与完整依赖均为 0 个已知漏洞                                                                               |
| 当前 Schema/实例校验                    | 4 类资源、5 个 Package、30 个 Artifact、2 个公开示例 Application 均可装载并通过当前规则                       |
| 发布包预览                              | `@coga/core` 可打包，预览包含 47 个文件；包内缺少 `LICENSE`                                                   |
| 公开边界与历史扫描                      | 当前工作树和 Git 历史未发现已知私有标识、密钥模式或 `private/` 提交记录                                       |
| 远端治理快照                            | HEAD 工作流通过；`main` 要求 `verify`、禁止强推/删除；Secret Scanning 与 Push Protection 开启；未配置代码扫描 |

这些结果证明当前候选可构建、可测试、当前内容未泄漏；它们不等于证明所有声明的治理语义都已由 Core 执行。

### 2.3 本次未声称完成的验证

- 未连接真实后端、生产数据或生产密钥。
- 未用微信开发者工具完成真实预览、真机或平台审核。
- 未执行发布、部署、npm 发布或目标环境操作。
- 未将本地 Application 视为生产就绪；其 manifest 明确属于 fixture-only、禁止发布的验证对象。

## 3. 当前架构与事实边界

| 层            | 当前事实来源                              | 当前已保证                                             | 不属于当前保证                                   |
| ------------- | ----------------------------------------- | ------------------------------------------------------ | ------------------------------------------------ |
| COGA Core     | TypeScript、JSON Schema、测试             | 资源装载、Schema、一部分跨资源检查、目录、直接影响查询 | 领域含义、任意测试执行、审批系统、完整契约注册表 |
| COGA Instance | `instance.yaml`、Package 与 Artifact 文件 | 有界领域、精确 Package 注册、共享知识清单              | 自动证明每条规则已被 Application 执行            |
| Application   | Application manifest 与实现代码           | 交付目标、精确 Harness 锁、产品选择                    | 重新定义共享规则或把声明自动变成运行时证据       |

规范优先级如下：

1. 对“当前行为”的描述，以可执行代码、Schema 和通过的测试为准。
2. README 和操作手册必须准确解释当前行为及限制。
3. 愿景、治理流程和本文件的目标设计描述未来状态，不可作为当前自动化保证。
4. 任何把目标升级为保证的改动，都必须同时提交实现、测试、迁移说明和文档。

## 4. 设计差异清单

优先级含义：P0 是进入下一次公开候选或处理不可信输入前必须解决；P1 是 0.2 语义承诺前必须解决；P2 是质量、可维护性和发布成熟度改进。

| ID   | 优先级 | 设计意图                                       | 当前实现与证据                                                                                                                                                                            | 对齐决策                                                                                                                    |
| ---- | ------ | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| D-01 | P0     | 公开白名单保证任何发布文件都经过隐私检查       | `public.release.json` 对示例目录按前缀放行；隐私脚本只扫描一组文本扩展名和固定模式，新增 CSV、PDF、图片或压缩包可进入边界而不做内容检查                                                   | 公开 Profile 对所有候选文件先分类；未知类型默认拒绝，二进制须显式登记并做元数据/人工证据检查                                |
| D-02 | P0     | Canonical resource 不携带明文秘密              | 当前启发式规则可发现典型敏感键和若干 token，但嵌套 `{ apiKey: { value: ... } }`、`authorization`、`credential` 等上下文可绕过                                                             | 扫描器继承祖先敏感上下文、规范化键名并扩充高置信模式；保留误报豁免证据                                                      |
| D-03 | P0     | 装载器对不可信本地输入失败可控                 | YAML 自引用别名可进入对象图，递归秘密扫描会触发栈溢出；当前也没有统一文件大小、深度和节点数上限                                                                                           | 装载后验证 JSON-like 无环结构；所有遍历使用 `WeakSet`，并增加文件、深度、节点和别名预算                                     |
| D-04 | P1     | Capability 的契约引用可复现、可验证            | Core 只要求 Capability 至少有一个 `contractRef`；`version` 可缺省，不检查文件存在、格式、ID 或版本。Core 的 valid fixture 也引用了不存在的契约文件                                        | 0.2 要求精确版本，装载并校验 OpenAPI/JSON Schema，验证路径边界、契约身份和可选摘要                                          |
| D-05 | P1     | 发布由策略、场景和证据共同约束                 | `approvalRequiredFor`、`policyRefs` 只是 Schema 字段；场景只检查目标存在及类型，不执行；发布只要求至少一条 provenance                                                                     | 把声明校验与证据执行分离；Core 验证证据记录完整性，受控 Runner/CI 产出不可变证据，外部审批系统负责人的决定不得由 Agent 代替 |
| D-06 | P1     | 影响分析返回直接、传递、旧版本消费者和路径     | `impact()` 仅按 Artifact ID 找所属 Package，再匹配直接锁定该精确 Package 的 Application；不遍历 Package 依赖、Artifact relation，也不区分输入 Artifact 版本                               | 构建版本化反向图，接受 `id@version` 或变更集，输出 direct/transitive/older-pin、完整路径和确定性排序                        |
| D-07 | P1     | 资源图形成合法闭包                             | 当前可以接受悬空治理策略、`Application` 使用 `core` scope、已发布 Package 包含 candidate Artifact、Package 依赖环，以及未由 Application Harness 覆盖的 Runbook/Scenario                   | 增加 Scope/生命周期矩阵、Package DAG、Package—Artifact 所有权闭包和 Application 绑定可达性检查                              |
| D-08 | P1     | `visibility` 与公开发布形成强边界              | `metadata.visibility` 可省略；只有显式 `public` 源会触发已装载资源间的直接可见性检查；路径可使用绝对路径或越界相对路径                                                                    | 默认本地 Profile 保持兼容；新增严格 public Profile，要求显式 visibility、根目录约束和传递公开闭包                           |
| D-09 | P1     | 本地 Application 能以证据证明关键 Harness 规则 | 权益对象没有过期、签名、发行者/受众信息，判定只检查来源和 grant；遥测缺少 owner/purpose/schemaVersion/retention/classification；UI 状态缺少 stale/denied/terminal；声明的场景没有执行证据 | 在 Application 侧实现类型化验证结果、最小遥测信封、完整状态机和逐场景契约测试；完成前不得把 manifest 声明视为合规证明       |
| D-10 | P1     | 本地 Application 的 fixture 行为与产品语义一致 | 第二条研究摘要可出现在列表中，但详情适配器只支持第一条；“每日投票”实际上按适配器生命周期限一次                                                                                            | 统一 fixture 数据源；把日期和持久化边界纳入 vote key，并添加跨日/重启测试                                                   |
| D-11 | P1     | 小程序构建与无障碍检查覆盖真实页面             | 构建脚本未要求页面 WXML/WXSS/JSON 和组件产物；测试未加载页面/组件/runtime/app；配置使用可变 `trial` 基础库；WXML 使用 `role` 而非平台 `aria-role`，多组普通文本对比度低于 4.5:1           | 固定基础库版本，完整校验产物，增加开发者工具编译/预览检查，改用平台 ARIA 属性并建立颜色 token 对比度测试                    |
| D-12 | P2     | 发布物可独立复用与追溯                         | npm 包尚未发布，GitHub Release 只有源码；包内缺 `LICENSE`，tag/HEAD 未签名，无 SBOM/构建 provenance                                                                                       | 是否发布 npm 由维护者决定；发布前补齐许可证、不可变构建产物、SBOM、provenance 和签名策略                                    |
| D-13 | P2     | 文档准确描述 0.1                               | README 将直接影响查询称为确定性反向影响；知识模型描述了 Schema 中不存在的 provenance/SKOS 字段；Agent 手册把人工检查写成 Core 自动检查；规范示例漏写必填 `contractRefs`                   | 本次已按“当前自动保证 / 人工或 Agent 审查 / 目标能力”重新标注，并修复示例                                                   |

当前没有发现已提交的私有内容或已知依赖漏洞。D-01 至 D-03 表示控制设计可被构造输入绕过，不表示当前仓库已经发生泄漏或远程利用。

## 5. 目标设计

### 5.1 三档校验 Profile

建议保留一个 API、三个明确的校验档位：

| Profile   | 用途                   | 行为                                                                                |
| --------- | ---------------------- | ----------------------------------------------------------------------------------- |
| `local`   | 0.1 兼容、本地可信仓库 | 当前 Schema 和跨引用检查；对旧资源保持兼容                                          |
| `public`  | 公开候选               | 要求显式 public visibility、根目录内路径、传递公开闭包、严格文件类型/大小和秘密扫描 |
| `release` | 不可变 Harness 发布    | 包含 public 全部规则，并要求契约、证据、审批引用、版本与摘要完整                    |

CLI 目标形式为 `coga validate <instance> --profile public|release`。库 API 使用显式 options，不能从环境变量静默改变语义。默认 Profile 的改变属于破坏性变更，只能进入新的 Schema/API 版本。

### 5.2 安全装载与公开边界

1. 装载前检查文件真实路径、类型和大小；public/release Profile 禁止逃逸 Instance 根目录。
2. YAML 解析后转换为有预算的无环 JSON-like 图；拒绝函数、特殊原型、自引用和超限别名。
3. 所有递归检查共享访问集合与最大深度，错误返回稳定 issue code，不抛出未捕获 `RangeError`。
4. 发布脚本先枚举所有候选文件，再应用精确文件或目录策略；未分类扩展名默认失败。
5. 文本统一按受控编码扫描；允许的二进制逐项登记来源、用途、摘要和人工检查证据。
6. 秘密检测同时考虑当前键、祖先键和内容模式；豁免必须绑定文件、规则、原因和到期时间。

### 5.3 版本化资源图

所有节点使用 `(kind, id, version)` 作为精确身份。至少建立以下有向边：

- Instance → Package、Instance → Application；
- Package → dependency Package、Package → owned Artifact；
- Application → locked Package、Application → Scenario/Runbook；
- Artifact → relation Artifact、Artifact → Scenario、Artifact → Contract；
- Governance → Policy。

图校验必须回答：目标是否存在且类型正确、版本是否精确、Scope 是否允许、源是否能通过其 Package 闭包到达目标、生命周期是否允许发布、是否存在禁止的环。错误输出必须包含源文件、JSON Pointer、目标身份和稳定 issue code。

### 5.4 契约注册与校验

0.2 的 `contractRef` 至少要求：

- `id`、`version`、`path`；
- 可辨识的 `format`（OpenAPI 3.1 或 JSON Schema 2020-12）；
- release Profile 下的内容摘要；
- 文件身份与引用身份一致，且路径位于允许根目录；
- OpenAPI/JSON Schema 自身通过对应规范校验。

为了避免 Core 猜测任意文件的业务语义，契约是否满足领域行为仍由场景或 Application 契约测试证明。Core 只负责身份、格式、闭包和证据引用。

### 5.5 生命周期、证据与审批

`validation` 不应只保存 `passed` 字样。发布态记录应要求：

- `checkedAt`、稳定的 `validator` 身份和至少一条 `evidence`；
- 证据引用不可变提交、CI run、测试报告或签名摘要；
- Scenario 的 Given/When/Then 声明与 Runner 结果分离；
- `approvalRequiredFor` 和 `policyRefs` 均能解析，并由外部审批证据确认；
- 证据过期规则由 Policy 声明，不在 Core 中硬编码某个行业周期。

Core 不直接执行 Artifact 中的任意命令。受控 Runner 只运行仓库预先登记的适配器，避免把数据文件变成代码执行入口。

### 5.6 传递影响分析

目标输出至少包含：

```text
changed artifact@version
  -> owning package@version
  -> dependent package@version (0..n)
  -> application@version
```

每个结果标记 `direct`、`transitive` 或 `older-pin`，保留完整路径，并列出需要重跑的 Scenario/Runbook。纯图结果由 Core 确定性生成；“权限扩大、失败语义改变”等语义判断仍由 Human/Agent 基于 diff 给出，不能与图可达性混为一项自动保证。

### 5.7 Application 证明接口

Harness 是规范，Application 必须提供实现证据。建议本地 Application 采用以下适配器边界：

- 权益：返回 `valid | expired | malformed | contradictory | unavailable` 的判定结果；在进入领域模型前验证发行者、受众、有效期和完整性证据。
- 遥测：统一信封强制 owner、purpose、schemaVersion、retention、classification，并对事件名和属性使用双重 allowlist。
- 页面状态：使用含 `loading | empty | ready | stale | denied | recoverable-error | terminal-error` 的穷尽联合类型；只有 recoverable error 提供重试。
- 场景证据：每个 Application manifest 场景绑定一个测试 ID 和最近通过的证据；无法执行的场景标为 manual，而不是预填 passed。
- 小程序交付：固定平台基础库，检查完整页面/组件产物，并把开发者工具编译、预览和真机结果作为不同证据，不互相替代。

## 6. 兼容与迁移方案

| 阶段                 | 兼容策略                                     | 主要变化                                                     |
| -------------------- | -------------------------------------------- | ------------------------------------------------------------ |
| 0.1 文档对齐（本次） | 无运行时变化                                 | 准确描述直接影响、声明式治理和当前字段                       |
| 0.1.1 安全加固       | 默认 `local` 行为兼容；新严格 Profile opt-in | 无环遍历、资源预算、公开文件分类、增强秘密扫描               |
| 0.2 语义闭包         | 新 Schema/API 版本；提供迁移器和双读期       | 契约版本必填、图闭包、生命周期/Scope/Policy 规则、版本化影响 |
| 0.2.x 证据执行       | Runner 插件按能力启用                        | 场景结果、审批与 Application 证据接入                        |

迁移工具应把缺失 `visibility` 标为待人工选择，而不是默认公开；给缺失的 `contractRef.version` 生成错误清单，而不是猜测；旧 `validation.status: passed` 在没有时间、验证器和证据时降级为 `unverified`。

## 7. 实施顺序与验收

### 阶段 A：0.1.1 安全门禁

- 修复循环对象崩溃和嵌套敏感字段绕过。
- 公开候选的每个文件都有分类结果，未知类型失败。
- 增加针对 CSV、PDF、图片、压缩包、路径逃逸、循环 YAML、深层对象和嵌套秘密的负向测试。
- 验收：现有有效 fixture 保持通过；所有构造绕过变成稳定的 validation issue；进程不崩溃。

### 阶段 B：0.2 语义闭包

- 实现契约注册、版本化资源图、Scope/生命周期矩阵和传递影响。
- 修复 Core valid fixture 中不存在的契约引用。
- 验收：悬空 Policy、Package 环、不可达 Runbook、published Package 包含 candidate Artifact、旧版本消费者均有确定结果和快照测试。

### 阶段 C：Application conformance

- 重构权益、遥测、状态和 fixture；建立 manifest 场景到测试证据的映射。
- 完成小程序完整构建、ARIA、颜色对比度和开发者工具检查。
- 验收：过期/伪造/畸形权益默认拒绝且不抛异常；遥测不能记录未登记字段；第二条研究内容可打开；每日投票跨日期与重启行为有测试；所有声明场景有证据或明确标为 manual。

### 阶段 D：发布成熟度

- 把 `LICENSE` 纳入 npm 包，确定是否正式发布 npm。
- 生成 SBOM、构建 provenance 和可验证签名；按风险决定是否增加代码扫描及 PR 审批要求。
- 验收：从干净 checkout 可重复生成内容相同的发布物，Release 资产、摘要、许可证和来源提交可互相追溯。

## 8. 非目标

- Core 不解释证券、微信、组织或产品业务规则。
- 不把 YAML 变成任意代码执行或通用 Policy DSL。
- 不引入第二套权威数据库；索引、图和表单仍是派生视图。
- 不把 Agent 判断当作审批、法律意见或运行证据。
- 不在没有版本与迁移说明时强化默认校验并破坏 0.1 使用者。

## 9. 设计完成定义

后续能力只有同时满足以下条件，才可以从“目标”移入 README 的“已包含”：

1. Schema/API 行为有版本化定义和兼容说明。
2. 正向、负向、边界和回归测试在受支持 Node.js 版本通过。
3. CLI、库 API、README、知识模型和 Agent 手册使用同一能力边界。
4. 公开边界、隐私和许可证检查覆盖新增文件类型。
5. Application 层声明附有可重放证据，人工检查项明确标识为人工。
6. 从干净 checkout 可以复现校验结果与发布候选。
